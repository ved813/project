rm -f .git/index.lock(if two commands are run symontaniously)
git 
git status 
git inti
git add .(to add all the files)
git commit -m "Name"(to save)
git log(who changed the file)
git config --global user.name "NAME"(all files in this PC will be used by this name and email)
git config --global user.email "email"
git remote remove origin(for not uploading or to change the repository)
git push -u origin master
git pull origin master 
git diff filename.format
git diff --stagged filename.format
git reset filename.format
git checkout filename.format(or . for bringing the file back)
git pull for any changes 
git add -A (stages all)
git add . (stages new and modified,without deleted)
git add -u (stages modified and deleted ,without new)
git clone SSHLINK(for taking any code from github the whole code)
ls -la(this will see all the files in this)
touch .gitignore(will add rules in this for ignoring files git haswebsite where it explains which files to ignore)
git branch 
git branch login(branch name you want to create login is example)
git checkout login(branch name you want to create login is example)
git push origin anyname
git merge login(branch name you want to create login is example and in which ever branch you want to merge run the code there)
git branch -d login(branch name you want to create login is example)
git push origin --delete login(branch name you want to create login is example this is for deleting from github)

